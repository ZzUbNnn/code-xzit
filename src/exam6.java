import java.util.Scanner;/** * @param * @author Administrator * @date 2021/3/31 0031 19:19 * @throws Exception */public class exam6 {    public static void main(String[] args) {        Scanner sc = new Scanner(System.in);        String str= sc.nextLine();        String[] s = str.split(" ");        boolean digit1 = isDigit(s[0]);        boolean digit2 = isDigit(s[1]);        if (!digit1){            s[0] = "?";        }        if (!digit2){            s[1] = "?";        }        String sum = "?";        if (digit1 && digit2 && s.length > 2){            System.out.println(Integer.parseInt(s[0]) + " + " + "? = " + sum);            return;        }        if (digit1 && digit2){            sum = String.valueOf(Integer.parseInt(s[0]) + Integer.parseInt(s[1]));        }        System.out.println(s[0] + " + " + s[1] + " = " + sum);    }    private static boolean isDigit(String s) {        boolean flag = true;        //判断是否都是数字        for (int i = 0; i < s.length(); i++){            if (!Character.isDigit(s.charAt(i))) {                flag = false;            }        }        int num = -1;        //如果都是数字，则解析为数字        if (flag == true){            num = Integer.parseInt(s);        }        //不在范围内        if (num < 1 || num > 1000){            flag = false;        }        return flag;    }    }